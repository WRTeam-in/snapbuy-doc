# Feature docs

Drop PDF / DOCX / TXT files here, then run `npm run rag:index`.
They get chunked, embedded, and become answerable by the AI assistant.
Files are also served as downloadable links (the answer "Sources" link to them).
